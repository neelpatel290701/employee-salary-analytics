export const InsightsPage = () => (
  <section>
    <h2 className="text-xl font-semibold">Insights</h2>
    <p className="mt-2 text-slate-600">
      The org-wide summary, country stats, job-title stats, headcount breakdown,
      and outlier panel land here in the TDD implementation phase.
    </p>
  </section>
);
